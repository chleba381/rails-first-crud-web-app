<%= form_with(model: director) do |form| %>
  <% if director.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(director.errors.count, "error") %> prohibited this director from being saved:</h2>

      <ul>
        <% director.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :first_name,"Jméno" , style: "display: block" %>
    <%= form.text_field :first_name, class:"form-control" %>
  </div>


  <div id="date_dropdown">
    <%= form.label :date_of_birth, "Datum narození", style: "display: block" %>
    <%= form.date_select :date_of_birth, { :include_blank => true, :start_year => 1900 }, { :class => "col-md-3" } %> 
  </div>
<br/>
  <div>
    <%= form.submit "Přidat režiséra", class:"btn btn-outline-success" %>
  </div>
<% end %>
